{
  "name": "sync-ecs",
  "version": "0.0.0",
  "description": "A library to sync ECS state",
  "scripts": {
    "lint:fix": "npx prettier --write 'src/**/*.ts' 'test/**/*.ts'",
    "test": "npx jest"
  },
  "prettier": {
    "singleQuote": true,
    "semi": false,
    "printWidth": 120
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}
